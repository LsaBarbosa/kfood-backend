spring:
  datasource:
    url: jdbc:h2:mem:kfood-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: kfood
    password: kfood

app:
  security:
    jwt-secret: test-secret
    jwt-expiration-seconds: 1800
